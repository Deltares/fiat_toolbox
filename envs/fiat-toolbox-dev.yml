name: fiat_toolbox-dev

channels:
  - conda-forge

dependencies:
  - black
  - ipykernel
  - numpy
  - pandas
  - pandasql<2.0
  - pip
  - pytest # tests
  - pytest-cov # tests
  - pytest-benchmark # tests
  - python>=3.7
