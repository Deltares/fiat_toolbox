name: fiat_toolbox

channels:
  - conda-forge

dependencies:
  - black
  - ipykernel
  - numpy
  - sqlalchemy < 2.0
  - pandas <= 2.0.0
  - tomli
  - pip
  - pytest # tests
  - pytest-cov # tests
  - pytest-benchmark # tests
  - python>=3.8
  - plotly
  - parse
  - seaborn # examples
  - pip:
    - duckdb
    - git+https://github.com/Deltares/Delft-FIAT.git
